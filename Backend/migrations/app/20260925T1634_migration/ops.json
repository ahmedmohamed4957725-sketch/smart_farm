[
  {
    "id": "column.public.users.updated_at",
    "label": "Add column \"updated_at\" to \"users\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "updated_at",
        "table": "users"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"updated_at\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "users",
          "updated_at"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"updated_at\"",
        "sql": "ALTER TABLE \"public\".\"users\" ADD COLUMN \"updated_at\" timestamp DEFAULT (now()) NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"updated_at\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "users",
          "updated_at"
        ]
      }
    ]
  }
]